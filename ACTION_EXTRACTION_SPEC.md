# 업무 단위 일정 추출 개선 명세서

- 작성일: 2026-09-07
- 대상: ms_automate / Solar Pro 4 / 클래식 Outlook COM
- 상태: v2 구현 기준 명세. 데이터 모델·DB·추출·UI·Outlook 연결에 반영됨.
- 근거: `test_set_30.xlsx`의 `테스트셋` 시트, 본문 G2:G33 및 발송일 F2:F33. 파일명과 달리 메시지는 32건이다.
- 사용자 프로필 기준: 중학교 2학년 3반 담임. 담당 교과·개별 시간표·선택적 행사 참여 여부는 알 수 없다.

## 1. 목적과 완료 목표

메시지를 날짜별로 분해하지 않고, 사용자에게 실제로 남아 있는 독립적인 행동 단위로 추출한다. 한 업무에 등장하는 행사일·접수 마감·참고 기간은 역할을 구분해 함께 보존한다.

대표 목표는 ‘토익 감독관 모집’에서 다음 결과를 만드는 것이다.

- 등록 후보: ‘토익 시험 감독관 신청’ 1건, 희망 시 수행하는 Todo.
- 시험일은 관련 행사 정보로 보존한다. 모집 공지만으로 감독 참석 일정을 만들지 않는다.
- ‘접수 마감 확인’이라는 별도 할 일을 만들지 않는다.
- ‘당일 오전 10시’의 날짜가 불분명하면 마감일 확인 상태로 남기고 10시는 보존한다.

독립적인 두 의무는 유지한다. 예를 들어 감독자로 지정된 교사의 사전 연수 참석과 실제 시험 감독은 별도 일정이다.

## 2. 범위

### 포함

1. Solar 추출 프롬프트와 구조화 출력 스키마 개선.
2. 행동 묶기, 수행 대상·조건·완료 상태 판정.
3. 발송일·인용문 문맥 및 날짜 역할 분리.
4. 마감 시각, 불명확한 시각, 관련 행사 정보의 모델·DB·JSON 보존.
5. 단순한 카드 UI에서 조건과 확인할 필드 표시.
6. 기존 Outlook 생성 및 중복 검사와의 연결.
7. 오프라인 대체 경로와 기존 데이터 호환.
8. 32건에 기반한 평가 기준과 회귀 테스트.

### 제외

- 기존 Outlook 항목의 자연어 조회·수정·삭제 UI.
- 실제 신청·메일 회신·문서 삭제 등 원문에 적힌 업무 자체의 실행.
- 쿨메신저 UDB 자동 탐색, 개인정보 탐지, 마스킹 복원.
- Excel 입력 UI 신설. 이번 Excel은 평가 자료로 사용한다.
- 매 메시지마다 여러 모델을 호출하는 다중 에이전트 구성.

## 3. 현재 구조와 수정 이유

| 위치 | 현재 확인된 동작 | 수정 요구 |
|---|---|---|
| `upstage_classifier.py` / FIELD_RULES | `at`과 `due` 동시 출력 금지, 여러 일은 분리 | 독립 행동을 먼저 정하고 관련 시점은 별도 정보로 허용 |
| `upstage_classifier.py` / TAXONOMY | 애매하면 추출, 재현율 우선 | 불확실한 후보는 보존하되 필수 업무처럼 자동 선택하지 않음 |
| `upstage_classifier.py` / `_normalise_date` | 마감의 날짜 부분만 보존 | 별도 `due_time`으로 시·분 보존 |
| `upstage_classifier.py` / `_to_classification` | at·due 동시 수신 시 at 제거 | 날짜 역할과 주 행동을 검증한 뒤 명시적으로 매핑 |
| `models.py` / `WorkItem.action` | due 우선, 다음 start로 대상 파생 | 검증된 명시적 `type`을 대상의 기준으로 사용. 시간 미확정 Calendar가 Todo로 바뀌지 않게 함 |
| `classifier.py` | 줄 단위 후보 분할 | 근거 없는 세분화를 줄이는 보수적 대체 처리 |
| `cards.py` / `to_work_item` | 새 WorkItem 재구성 | 숨긴 필드와 기존 end·all_day·category·reminder 등까지 보존 |
| `service.py` | 제목·마감 및 시간 겹침 중심의 중복 검사 | 의미상 묶기는 등록 전 추출 단계에서 수행. Outlook 중복 검사를 대체하지 않음 |
| `review.py` | 기본 기준일이 오늘 | 발송일·사용자 지정일·오늘을 출처와 함께 구별 |

현재 오류의 내부 추론을 확인한 것은 아니다. 위 구조는 관찰된 과도한 분리와 마감 시각 누락을 설명하는 개선 지점이다.

## 4. 핵심 판정 규칙

### R01. 독립 행동 기준

‘한 항목을 완료해도 다른 행동이 별도로 남는가’를 분리 기준으로 사용한다. 날짜 수, 문장 수, 번호 개수만으로 분리하지 않는다.

- 분리: 사전 연수 참석 / 본 시험 감독.
- 분리: 종례까지 충전 확인 / 종례 후 책상 배열.
- 통합: 감독관 신청 / 신청 마감 확인.
- 통합: 한 조사표에 수량·그물망 여부·비고 입력 → 조사표 작성 1건, 세부 항목은 체크리스트.
- 통합 가능: 같은 시트·같은 마감의 월중·주간 계획 입력 → 기본은 1건과 하위 체크리스트. 별도 산출물이나 별도 완료 관리가 명시되면 분리.
- 같은 행사라는 이유만으로 서로 다른 의무를 강제로 합치지 않는다.

### R02. 내 행동과 다른 사람의 행동

행동의 수행자를 사용자 프로필과 메시지 지시로 판단한다.

- 학생에게 행사 안내 요청 → 교사의 안내 업무. 학생의 행사 참석을 교사 일정으로 만들지 않는다.
- 발신자의 공문 제출·기관의 접수 처리 → 내 행동의 배경 정보.
- ‘주변에 추천할 분 있으면 연락’ → 조건부 추천·회신 후보.
- 담당자·대상 학년이 불명확하면 `unknown`으로 남긴다. 비식별화된 이름으로 담당자를 추측하지 않는다.

`scope`는 유지한다. 담임은 학생 전달·지도 행동, 업무는 교사 본인의 제출·입력·참석, 참조는 별도 수행 행동이 없는 내용이다. 학생을 위한 사업이라도 내가 조사표를 작성하는 행동 자체는 업무가 될 수 있다.

### R03. 선택적 모집과 확정 참석

- 희망자 모집·참가 신청 안내 → 신청/회신 후보 1건, 조건부로 표시하고 기본 선택 해제.
- ‘감독교사로 지정되었습니다’, ‘반드시 참석’ → 대상이 맞으면 참석 의무.
- ‘신청과 동시에 참여 확정’이라는 제도 설명만으로 사용자가 이미 신청했다고 판단하지 않는다.
- 사용자가 신청 카드를 체크하는 것은 Todo 등록 선택일 뿐, 실제 참가 신청 완료가 아니다.
- 별도 참석 일정이 필요하면 명시적인 사용자 요청 또는 참가 확정 근거가 있을 때만 생성한다. 이번 범위에서 자동 후속 일정 생성은 구현하지 않는다.

### R04. 같은 업무의 날짜 역할

모든 시점은 다음 중 하나로 분류한다.

| 역할 | 뜻 | 기본 등록 연결 |
|---|---|---|
| execution | 내가 직접 수행·참석할 시각 | Calendar의 start/end |
| deadline | 내 행동의 명시된 마감 | Todo의 due/due_time |
| event_context | 신청·안내의 대상 행사일 | 관련 일정, 설명에만 포함 |
| external_deadline | 기관·다른 담당자의 마감 | 관련 정보. 내 마감으로 대체하지 않음 |
| constraint | 내 행동에 영향을 주는 기간·상한 | 관련 정보. 내 명시 마감과 구별 |
| historical | 과거 실적·인용·제도 시작일 | 배경 정보 |

예: 내부 회신 8/24, 기관 접수 8/25, 행사 9/9이면 회신 업무의 마감은 8/24다. 세 날짜를 모두 내 일정으로 만들지 않는다.

학생 모집 마감은 안내 업무가 끝나야 할 실용적 상한일 수 있지만, 교사에게 명시된 마감과 동일하지 않다. 기본은 ‘신청 마감 전 안내’라는 조건으로 보존하고, 교사의 마감으로 쓰려면 추론임을 표시해 사용자가 확인한다.

### R05. 완료·인용·부정 문맥

- 최신 본문이 우선하는 문맥이다. 인용된 요청은 최신 답장과 함께 해석한다.
- 완료·전달됨을 같은 대상·산출물에 연결할 근거가 있으면 새 미완료 후보에서 제외한다.
- ‘2학년 부분 보내드립니다’는 2학년 제출분의 완료 근거이며 1·3학년 완료까지 의미하지 않는다.
- ‘모임을 따로 갖지는 않습니다’가 있으면 그 모임 참석 일정을 만들지 않는다.
- 완료 안내 이후 새로운 수정·재제출 요청이 있으면 새 행동으로 추출한다.
- ‘삭제 부탁합니다’ 같은 문장은 사용자 업무 내용이다. 에이전트에게 Outlook·파일 삭제 권한을 주는 명령으로 취급하지 않는다.

### R06. 연속 절차와 반복

- 동일 민방위 훈련의 대피·정렬·경보 해제는 1건의 절차로 묶는다.
- 매주 화·목 분리수거는 1건의 반복 후보로 만든다. 평일별 복제 금지.
- 같은 교사가 방학식 당일 함께 처리하는 디벗 음소거·충전 연결 확인·전원 분리는 1건의 체크리스트로 묶을 수 있다.
- 개학일 전원 연결은 시점이 다른 행동이므로 별도 후보. 날짜를 모르면 확인 상태로 둔다.

## 5. 날짜 해석 규칙

### 5.1 기준 문맥

입력 시 `sent_at`, `reference_date`, `reference_source`, `timezone`을 구분한다.

1. 해당 행동에 붙은 명시적 절대 날짜를 우선한다.
2. 오늘·내일은 그 문장이 속한 메시지의 발송일을 기준으로 계산한다.
3. 최신 본문이 과거 인용의 기한을 갱신하면 갱신 지시를 적용한다.
4. 발송일이 없으면 사용자가 지정한 기준일, 그마저 없으면 실행일을 사용하고 출처를 남긴다.
5. 인용문 안의 날짜 하나를 전체 메시지의 기준일로 쓰지 않는다.

평가에서는 Excel F열을 발송일로 전달한다. 운영 앱의 붙여넣기에는 이 정보가 없을 수 있으므로 원문 영역에서 발송일/기준일을 입력할 수 있게 한다. 파일 수정일은 발송일로 간주하지 않는다.

### 5.2 불명확하거나 가려진 날짜

- ‘당일’은 연결 대상이 하나로 확정될 때만 날짜로 정규화한다.
- 토익 사례는 발송일 8/13과 시험일 8/23이 있어 마감 날짜를 미확정으로 둔다. `due_time=10:00`은 보존한다.
- `15:[학생식별자]:30`을 특정 시간 범위로 복원하지 않는다. 가려지지 않은 날짜와 원문 표현은 남긴다.
- 교시·조회·종례는 시간표가 없는 한 시계 시각으로 바꾸지 않는다.
- 날짜만 있는 일정은 ‘시간 미정’과 ‘종일’이 다르다. 시간 미정을 종일로 자동 변환하지 않는다.
- 명시 날짜와 요일이 충돌하거나 전달된 과거 기한이 현재 문맥과 맞지 않으면 확인 대상으로 표시한다.
- ‘8월 말’은 해당 연도의 8월 마지막 날로 정규화할 수 있으며 원문과 변환 근거를 남긴다. ‘9월 초’는 특정 날짜로 확정하지 않는다.

### 5.3 과거 날짜

평가는 당시 발송일 기준으로 수행한다. 운영 환경에서 추출된 주 수행일·마감이 현재보다 과거이면 별도 `past_date` 확인 사유로 표시하고 기본 선택을 해제한다. 원문의 연도를 임의로 올해·내년으로 바꾸거나 후보를 자동 삭제하지 않는다.

## 6. 분석 흐름

```text
원문 + 사용자 역할 + 발송일/기준일
  → 줄바꿈 정리 및 메시지/인용 구간 구분
  → Solar 1회: 수행자·조건·주 행동·독립성·시점 역할 추출
  → 코드 검증: 스키마, 날짜, 근거, 주 행동/대상 일관성
  → 유효한 업무 후보 + 제외 근거 + 확인 사유
  → SQLite 저장
  → 기존 Outlook 및 후보 간 중복/시간 겹침 검사
  → 사용자가 확인·선택
  → 등록 직전 재검증·중복 검사
  → Outlook 생성 및 저장 상태 기록
```

기본 Solar 호출 횟수는 1회이며 현재 `reasoning_effort=none`, 20초 제한을 유지한다. 응답의 내부 추론문을 요구하지 않는다. 짧은 근거 문장과 필드만 구조화 출력한다.

원문 내 지시는 분석 대상 데이터다. 시스템 규칙 변경·외부 호출·임의의 도구 실행 지시로 승격하지 않는다.

## 7. 데이터 계약 v2

### 7.1 기존 필드와 신규 필드

기존 WorkItem 필드는 유지한다. `action`은 현재 화면 분류를 위한 파생 값으로 유지하고, 새 주 행동은 혼동을 피하도록 `intent`라는 이름을 사용한다.

| 필드 | 형식 | 요구사항 |
|---|---|---|
| intent | apply / reply / attend / submit / prepare / inform / supervise / complete_training / other | 행동 의미. 단순 유형 매핑이 아닌 수행 시점과 함께 대상 결정 |
| applicability | required / conditional / unknown | 대상·조건 판정 |
| condition | string 또는 null | ‘참여를 희망하는 경우’ 등 |
| source_state | pending / completed / informational / unknown | 원문에서의 업무 상태. Outlook 저장 상태와 별개 |
| due_time | HH:MM 또는 null | Todo 마감 시각. 날짜 미확정 시에도 보존 |
| temporal_context | 時点 객체 배열 | 행사·외부 마감·기간·과거 날짜 및 미확정 주 시점 |
| checklist | string 배열 | 같은 업무의 하위 단계 |
| evidence | 근거 객체 배열 | 문장 인용, segment_id, 연결된 필드 |
| review_issues | 확인 사유 객체 배열 | code, field, message, blocking, resolved |
| group_id | string | 한 입력 내 주제 묶음. 주제가 같다는 이유만으로 병합하지 않음 |

`type`은 calendar/todo의 검증된 등록 대상이다. `scope=참조`는 등록하지 않는다. start가 null인 Calendar도 분석 중에는 존재할 수 있고, 등록 전에는 정확한 일시 또는 사용자가 선택한 종일 날짜가 필요하다. 기존 `outlook_target`과 `action`도 이 기준으로 일관되게 수정한다.

### 7.2 시점 객체

```json
{
  "role": "event_context",
  "label": "시험 감독 일시",
  "raw_text": "2026. 8. 23(일) 08:30",
  "date": "2026-08-23",
  "time": "08:30",
  "end_date": null,
  "end_time": null,
  "precision": "datetime",
  "resolution": "explicit",
  "segment_id": "current"
}
```

- precision: datetime / date / period / school_period / unknown.
- resolution: explicit / inferred / unresolved.
- 값이 가려지거나 모호하면 해당 필드는 null. raw_text는 보존한다.
- 주 execution/deadline이 확정되면 start/end 또는 due/due_time이 저장·편집의 기준이다. temporal_context에는 관련 시점과 미확정 주 시점만 두어 두 곳의 값이 어긋나지 않게 한다.
- evidence는 원문에 존재하는 구절인지 코드로 검증한다. 코드가 확인할 수 없는 의미 판단은 검증 완료라고 표시하지 않는다.

### 7.3 토익 사례의 예상 후보

아래는 사용자가 붙여넣은 시간 정보가 온전한 원문 기준이다. Excel의 가려진 시험 시간에는 적용하지 않는다.

```json
{
  "schema_version": 2,
  "items": [
    {
      "type": "todo",
      "scope": "업무",
      "intent": "apply",
      "title": "토익 시험 감독관 신청",
      "applicability": "conditional",
      "condition": "참여를 희망하는 경우",
      "source_state": "pending",
      "start": null,
      "end": null,
      "due": null,
      "due_time": "10:00",
      "selected": false,
      "description": "수당 8만원. 선착순 12명. 대리 접수 불가.",
      "temporal_context": [
        {
          "role": "event_context",
          "label": "시험 감독 일시",
          "raw_text": "2026. 8. 23(일) 08:30",
          "date": "2026-08-23",
          "time": "08:30",
          "end_date": null,
          "end_time": null,
          "precision": "datetime",
          "resolution": "explicit",
          "segment_id": "current"
        },
        {
          "role": "deadline",
          "label": "접수 마감",
          "raw_text": "당일 오전 10시까지",
          "date": null,
          "time": "10:00",
          "end_date": null,
          "end_time": null,
          "precision": "unknown",
          "resolution": "unresolved",
          "segment_id": "current"
        }
      ],
      "review_issues": [
        {
          "code": "ambiguous_deadline_date",
          "field": "due",
          "message": "당일이 어느 날짜인지 확인해 주세요.",
          "blocking": true,
          "resolved": false
        }
      ],
      "checklist": [],
      "evidence": [
        {"segment_id": "current", "field": "intent", "quote": "토익 시험 감독관을 선착순으로 모집합니다."}
      ],
      "group_id": "toeic-invitation"
    }
  ]
}
```

이는 신규 필드 중심의 계약 예시다. 실제 strict JSON Schema는 기존 location·알림·반복 등도 포함하고 모든 객체에 additionalProperties=false 및 일관된 required 목록을 정의한다. `selected`는 LLM을 신뢰하지 않고 코드에서 산출하며 LLM 응답 스키마에는 넣지 않는다.

### 7.4 기본 선택 및 등록 가능 여부

- required + pending + 차단 사유 없음 + 중복 아님 → 기본 선택.
- conditional 또는 unknown → 후보는 보존, 기본 선택 해제.
- completed 또는 informational → 접힌 제외 영역, 등록 대상 아님.
- 미해결 blocking 사유 → 등록 불가. 다른 정상 후보의 등록은 허용.
- 조건부 후보의 사용자 체크는 해당 업무가 본인에게 해당함을 확인한 것으로 기록한다.
- 날짜 미확정은 날짜를 입력하거나, ‘날짜 없는 할 일로 보관’을 사용자가 명시적으로 선택해야 해소된다. Calendar를 날짜 없는 Todo로 바꿀 때는 사용자 변경으로 기록한다.
- 단순 날짜 수정으로 대상 불명·시각 손상 등 모든 확인 사유를 한꺼번에 해제하지 않는다.
- related 행사 시각만 가려졌고 신청 마감은 명확하면 신청 Todo 등록까지 막지 않는다. 영향받는 필드에 한해서 차단한다.

## 8. UI 요구사항

Microsoft 계열 파란 테마와 짧은 기본 화면을 유지한다.

### 기본 카드

```text
□ 토익 시험 감독관 신청               수정
  희망 시 · 마감일 확인 필요
```

- 제목, 주 시점, 조건 또는 확인 사유만 기본 표시한다.
- 관련 시험일은 펼친 상세에서 ‘관련 일정’으로 보여준다. 독립 카드로 늘리지 않는다.
- 상세 편집에서 마감 날짜와 시각을 분리 입력한다.
- 원문 발송일/기준일은 원문 펼침 영역에 배치한다.
- 원문·기준일을 바꾸면 ‘다시 분석 필요’ 상태로 전환하고 이전 결과의 등록을 막는다.
- 손상된 시각·담당자 불명 등 확인할 내용은 상세에서 원문 근거와 함께 표시한다.
- 완료·참조·중복은 접힌 제외 영역에서 확인 가능해야 한다.
- 등록 버튼은 실제 선택되고 등록 가능한 수를 반영해 ‘2건 등록’으로 표시한다.
- 후보 수정 시 분석에서 받은 새 필드와 기존 종료·종일·범주·알림·반복을 유실하지 않는다. 가능하면 dataclasses.replace 방식으로 변경 필드만 갱신한다.
- 일정과 Todo 간 대상 변경 시 비호환 필드를 조용히 버리지 않는다. 관련 정보로 보존하거나 사용자에게 변경 내용을 표시한다.

## 9. 저장 및 Outlook 연결

### 9.1 SQLite

기존 work_items·inputs 테이블을 유지하고 추가 컬럼으로 마이그레이션한다.

- inputs: `context_json TEXT NOT NULL DEFAULT '{}'` 추가. sent_at, reference_date, reference_source, timezone, schema_version, prompt_version, model을 저장한다.
- work_items: `due_time TEXT` 및 `semantic_json TEXT NOT NULL DEFAULT '{}'` 추가.
- semantic_json에 intent, applicability, condition, source_state, temporal_context, checklist, evidence, review_issues, group_id와 사용자 확인 기록을 저장한다.
- DB 마이그레이션은 중복 실행 가능해야 하고 기존 id·saved·EntryID·원문·필드를 보존한다.
- 기존 레코드는 v1 경로로 읽는다. ‘옛 모델에는 정보가 없음’을 의미하는 기본값을 쓰며 이미 저장된 항목을 새 후보로 되살리지 않는다.
- 날짜/시각 편집, 확인 사유 해소, 성공/실패 상태가 저장·재로딩 후 동일해야 한다.
- created_at은 입력 수집 시각이며 원문 발송일로 재사용하지 않는다.

### 9.2 Outlook 생성

- Calendar: 검증된 주 execution만 start/end로 전달한다. 날짜만 아는 미정 시간을 임의의 00:00으로 등록하지 않는다.
- Todo: due 날짜를 기존 DueDate 매핑으로 전달한다. 정확한 마감 시각은 due_time과 설명에 보존한다.
- Outlook Task 화면이 마감 시각을 지원한다고 가정하지 않는다. 기본 설명에 ‘마감: YYYY-MM-DD HH:MM’을 명시한다.
- 별도 알림을 사용자가 설정하면 기존 ReminderTime으로 연결한다. 마감 시각을 자동으로 알림 시각으로 대체하지 않는다.
- 관련 행사 날짜·체크리스트·조건은 설명에 포함한다. 설명 합성은 한 곳에서 수행하여 반복 등록 시 중복 문구를 쌓지 않는다.
- 신청 Todo를 등록하는 작업은 실제 신청이나 Calendar 참석 확정이 아니다.

### 9.3 중복 검사

- 의미적 묶기는 LLM 추출 단계에서 수행하며, 제목 유사도만으로 사후 병합하지 않는다.
- 기존 Calendar 시간 겹침과 Todo 제목+마감일 규칙은 유지한다.
- 관련 행사일은 Calendar 시간 겹침 검사 대상에서 제외한다.
- Todo 마감 시각을 추가해도 기존 날짜 기준 중복 판정을 바꾸지 않는다. 서로 다른 두 업무가 같은 제목·날짜여서 제외되면 사용자가 구체적인 제목으로 수정할 수 있다.
- 수정 후 및 실제 등록 직전에 기존 방식대로 중복 검사한다.

## 10. 코드 변경 명세

| 파일 | 구현 작업 |
|---|---|
| `upstage_classifier.py` | v2 스키마, R01~R06과 날짜 규칙, 주 행동 먼저 추출, 인용 문맥·조건·근거 출력, 새 필드 파싱 |
| `models.py` | 신규 의미 필드·시점 객체·확인 사유, 명시적 대상 및 action 일관성, v1 호환, 등록 전 유효성 검증 |
| `service.py` | 입력 context 전달, 코드 검증, 기본 선택 정책, 등록 가능 건수, 설명 합성, 확인 사유 검사 |
| `classifier.py` | 동일 출력 계약의 보수적 오프라인 후보. 문맥을 해석하지 못한 경우 확인 표시 |
| `storage.py` | 추가 컬럼 마이그레이션, context/semantic JSON 직렬화, due_time 저장 및 수정/읽기 경로 |
| `cards.py` | 조건·확인 배지, 관련 일정 및 마감 시각 편집, 기존·신규 필드 보존 |
| `review.py` | 기준일 출처, 미재분석 상태, 선택 수, 제외 영역, 등록 전 차단 사유 안내, JSON v2 내보내기 |
| `app.py` | 발송일 등 제공 가능한 입력 문맥 전달. 기본 붙여넣기/드롭 흐름 유지 |
| `outlook_adapter.py` | 시간 미확정 Calendar 등록 거부, 기존 DueDate/ReminderTime 매핑과 설명 전달 검증 |
| `README.md`, `IMPLEMENTATION_REPORT.md` | 행동 단위 추출, 희망 시 선택, 날짜 확인, 버전 호환 및 평가 결과 반영 |

추출 스키마를 바꾸면 API 응답 변환·DB·카드 편집·JSON 내보내기·Outlook 생성 경로를 함께 수정한다. 프롬프트만 바꾼 상태를 완료로 보지 않는다.

### 오프라인 동작

명확한 구조화 입력은 기존 기능을 유지한다. 모집·전달·복수 날짜 등 의미 해석이 필요한 입력은 줄마다 Calendar를 생성하지 않는다. 주제 단위로 보수적인 후보와 원문을 보존하고 ‘오프라인 분석 · 확인 필요’를 표시한다. 완전한 의미 분석 동등성을 약속하지 않는다. API 실패 시 의미 필드가 없는 v1 결과를 v2 확정 결과처럼 표시해서는 안 된다.

## 11. 32건 평가 기준

다음은 본문을 읽고 정한 권장 기대 결과다. 원본 엑셀을 수정하지 않고 별도 평가 fixture로 작성한다. 행 번호는 Excel의 실제 행이며 데이터 번호가 아니다. 조건·불확실성이 남는 사례는 무조건 단일 숫자로 채점하지 않는다.

| 행 / 본문 셀 | 사례 | 기대 행동 및 금지 오류 |
|---|---|---|
| 2 / G2 | 작가 만남 학생 모집 | 학생 안내 1건. 행사일과 학생 신청기한은 관련 정보. 교사 참석 생성 금지. 가려진 시간 복원 금지 |
| 3 / G3 | 교과협의록 요청 뒤 2학년 부분 송부 | 새 미완료 업무 0건이 기본. 해당 부분 완료 근거 보존. 다른 학년 완료까지 단정 금지 |
| 4 / G4 | 상담 가통 배부·상담주간 운영 | 배부와 상담 운영 2건. 발신자의 학급함 투입은 내 업무 아님. 가려진 기간 확인 |
| 5 / G5 | 국어 교사 추천 요청 | 추천·연락 1건, 조건부. 병가 기간·과거 공고를 일정으로 만들지 않음 |
| 6 / G6 | 월중·주간 계획 입력 | 같은 마감의 입력 1건+체크리스트 2개를 기본. 기간명을 참석 일정으로 만들지 않음 |
| 7 / G7 | 독서나눔 출장 중복 여부 질의 | 중복 여부 확인·회신 1건. 모임은 없다고 명시했으므로 모임 참석 생성 금지 |
| 8 / G8 | 복무 일반 안내 | 참조, 새 실행 업무 0건. 근무 원칙을 일회성 Todo로 만들지 않음 |
| 9 / G9 | 워크숍 희망 회신 | 조건부 회신 1건. 내 마감 8/24, 기관 마감 8/25·행사 9/9는 관련 정보 |
| 10 / G10 | 민방위 훈련 | 참여·지도 1건. 대피/경보 해제로 분리 금지. 교과 담당 여부·가려진 시간은 확인 |
| 11 / G11 | 디벗 충전·책상 배열 | 2건. 종례까지와 종례 후를 구별. 시계 시각 추측 금지 |
| 12 / G12 | 학교 수업 참관 희망 회신 | 조건부 회신 1건, 8/21 마감. 미정 참관일 생성 금지 |
| 13 / G13 | 문서 편철·회수 문서 삭제 | 서로 다른 대상의 행동 2건. 실제 문서 삭제를 실행하지 않음 |
| 14 / G14 | 오늘 1시 연수 | 참석 1건, 발송일 8/19와 문맥상 오후 13시. 시각 해석 근거 보존 |
| 15 / G15 | 2025년 명찰·학생증 신청 전달 | 수합·비용·서식 제출을 1건+체크리스트로 기본 묶음. 희망자 없음 회신은 대안 절차. 옛 마감은 확인 필요 |
| 16 / G16 | 감독자 지정·사전 연수·검사 | 필수 연수와 감독 2건. 권장 영상은 준비 체크리스트. 1~4교시는 시간표 확인 |
| 17 / G17 | 학년별 스포츠 배정 | 2학년 학급 대기 안내 1건, 8/24 적용. 1·3학년 일정을 내 일정으로 추가 금지 |
| 18 / G18 | 교직원 회의·연수 | 참석 1건, 8/25 시간 미정. ‘참고’라는 단어만으로 참조 처리 금지 |
| 19 / G19 | 매주 화·목 분리수거 | 반복 지도·배출 1건. 아침 시각 확인, 요일별 카드 복제 금지 |
| 20 / G20 | 리더십 캠프 가통 배부 | 8/18 조회 배부 1건. 8/20 캠프 참석 일정 추가 금지 |
| 21 / G21 | 케이블 교체 완료·주의 | 참조, 새 실행 업무 0건. 완료된 교체를 내 Todo로 생성 금지 |
| 22 / G22 | 의무연수 이수·시트 표시 | 이수 후 표시를 1건+단계로 기본 묶음. 이미 이수한 사람은 표시만. 9월 보고는 발신자 배경 |
| 23 / G23 | 토익 감독관 모집 | 조건부 신청 1건. 마감 확인 필요. 시험 참석·마감 확인 별도 생성 금지. Excel의 가려진 시험 시간은 복원 금지 |
| 24 / G24 | 소방기기 점검 안내 | 참조, 새 실행 업무 0건. 외부 점검자의 일을 교사 업무로 만들지 않음 |
| 25 / G25 | 교시별 행사 임장 | 본인 임장에 해당하는 교시만 후보. 마스킹으로 담당자 확인 불가 시 조건 표시. 4개 전부 확정 선택 금지 |
| 26 / G26 | 방학 디벗 관리 | 방학식 정리 1건+체크리스트, 개학일 재연결 1건. 다른 교사의 주간 관리 제외, 개학일 확인 |
| 27 / G27 | 방학식 생활안내 배부 | 7/23 4교시 배부 1건. 가려진 시간은 미정. 발신자의 이후 e알리미 발송 제외 |
| 28 / G28 | 안전연수 미이수·실적 누락 | 본인 상태에 따라 이수 또는 실적 보완. 상태 불명 시 대안 조건으로 보존하고 둘 다 필수 선택 금지. 제도 시작일 일정화 금지 |
| 29 / G29 | 검토의견서 송부 | 조건부 제출 1건, 7/16 15:00 보존. ‘있으면’ 조건 유지 |
| 30 / G30 | 걸레받이함 현황 조사 | 조사 입력 1건+체크리스트. 배경의 비상 대응 설명을 지금 실행할 업무로 만들지 않음 |
| 31 / G31 | 급식 설문 학생 안내 | 학생 안내 1건. 설문 종료 7/15 15:00은 안내의 관련 상한. 학생 설문 참여를 교사 업무로 만들지 않음 |
| 32 / G32 | 성적 일람표 배부·정정 안내 | 인쇄·배부·확인 지도 1건, 정정 필요 시 후속 지도 1건. 오늘/내일/확인 종료일을 구별 |
| 33 / G33 | 학년도 일시 전환 안내 | 참조, 새 실행 업무 0건. 발신자의 전환을 수신자 Todo로 만들지 않음 |

특히 G25처럼 역할이 가려진 사례는 후보 수 하나를 정답으로 고정하지 않고 ‘모든 교시를 내 의무로 단정하지 않음’을 채점한다. G28은 본인 이수 여부가 없어 조건부 분기 보존을 평가한다.

## 12. 검증 및 합격 조건

### 12.1 결정적 코드 테스트

API·실제 Outlook 없이 고정 JSON과 가짜 어댑터, 임시 DB로 수행한다.

1. v2 파싱 → 저장 → 재로딩 → 카드 편집 → JSON → 등록 전달에서 신규 필드와 기존 end/all_day/category/reminder가 보존된다.
2. v1 DB를 두 번 마이그레이션해도 id·EntryID·건수·저장 상태가 유지된다.
3. Calendar 시간 미정은 Todo로 자동 변경되지 않으며, 해결 전 등록 호출이 차단된다.
4. due_time=10:00/15:00이 UI·DB·JSON·Outlook 설명에 남는다. 미확정 날짜는 임의로 채우지 않는다.
5. 조건부·대상 불명·완료·중복·필드 확인 상태의 선택 정책을 검증한다.
6. 정상 항목과 확인 필요 항목이 섞여 있어도 정상 선택 항목만 등록할 수 있다.
7. 관련 행사일은 Calendar 겹침 검사에서 제외하고, 독립 참석 일정은 검사한다.
8. 입력·기준일 변경 후 재분석 전 등록 불가, 오래된 비동기 응답이 최신 결과를 덮어쓰지 않음.
9. API 실패 시 오프라인 후보의 한계 표시와 동일 저장 계약 유지.
10. 테스트에서 실제 Outlook 생성·수정·삭제를 하지 않는다.

### 12.2 Solar 품질 평가

- 원본 Excel은 보존하고 별도 fixture에 발송일, 본문, 사용자 역할, 기대 행동, 필수 조건, 금지 추출을 기록한다.
- 개발 예시와 검증용 메시지를 분리한다. 프롬프트 작성에 사용한 32건은 회귀 평가로 보고, 새 메시지 일반화 성능이라고 표현하지 않는다.
- 변경 전·후 프롬프트에 동일 입력을 사용한다. 외부 API 평가에는 모델·프롬프트 버전, 응답 시간, 실패/오프라인 전환을 기록한다.
- 토익 원문과 Excel 마스킹 원문을 별도 케이스로 평가한다.
- 평가 시 현재 날짜가 아닌 메시지 발송일 기준으로 비교한다. 운영 환경의 과거 날짜 경고 테스트는 별도로 한다.
- 지표: 독립 행동 누락, 불필요한 분리, 잘못된 수행자, 잘못된 날짜 역할, 근거 없는 시간 보완, 조건/완료 상태 누락.
- 모호한 사례에는 단일 문자열 제목·단일 개수를 강요하지 않는다. 필수 의미 조건과 금지 결과로 평가한다.
- 주요 사례 G9/G11/G16/G23은 각 3회 평가해 변동을 기록한다. 토익 1건, 연수+감독 2건, 내부 회신 마감, 충전+배열 분리가 반복해서 유지되어야 한다.

### 12.3 릴리즈 합격 기준

- 결정적 코드 테스트 전체 통과.
- 확정 판정 가능한 fixture의 필수 의미 조건 통과. 애매한 케이스는 확인 표시와 추측 금지 조건 통과.
- 주어진 모집 사례에서 확정 참석 일정 생성, 마감 시각 누락, 가려진 시간 복원, 완료 요청 재생성을 허용하지 않는다.
- 기존 업무 누락이 증가한 케이스가 있으면 원인과 기대 결과를 검토한 뒤 해결한다.
- 기본 Solar 1회 호출과 20초 제한을 유지하고 변경 전후 실제 응답 시간을 보고한다. 측정하지 않은 개선율·성공률은 쓰지 않는다.
- 문서와 실제 UI의 버튼명·상태·저장 결과가 일치한다.

## 13. 구현 순서

1. 32건의 기대 결과 fixture 및 현재 결과 기준선 확보.
2. 데이터 모델·DB 추가 컬럼·v1 호환·직렬화 구현.
3. 프롬프트/스키마 v2·정규화·근거/날짜 검증·오프라인 계약 적용.
4. 서비스의 선택/등록 정책과 기존 중복 검사 연결.
5. 카드 UI·원문 기준일·JSON v2·Outlook 설명 연결.
6. 결정적 테스트, Solar 비교 평가, UI 필드 보존 확인.
7. README 및 구현 리포트 갱신 후 릴리즈.

DB 변경 전에는 백업을 만들고, 신규 버전 기능을 끌 경우 기존 v1 분석 경로로 돌아갈 수 있게 한다. v2 레코드에 미해결 확인 사유가 있으면 v1 경로로 우회 등록하지 않는다. 과거 등록 기록의 자동 재분석·재등록은 수행하지 않는다.
