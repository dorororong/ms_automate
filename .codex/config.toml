# Project-local MCP configuration for the local classic-Outlook automation app.
# The app itself uses pywin32/COM and does not call Microsoft Graph.

[mcp_servers.microsoft_learn]
url = "https://learn.microsoft.com/api/mcp"
