# CATMOA식 업무 → Outlook 파이프라인 구현 리포트

작성일: 2026-09-07  
대상 프로그램: `ms_automate`  
참고 UX: [CATMOA 저장소](https://github.com/progh2/catmoa)

2026-09-08 추가: 프롬프트·스키마 간소화와 실측 결과는
[LATENCY_REPORT.md](LATENCY_REPORT.md)에 기록했습니다. 현재 기본 프롬프트는
`semantic-v4-compact`이며, 아래 8장의 32건 결과는 간소화 전 기준선입니다.

## 1. 구현 요약

기존 텍스트 중심의 작은 실행 창을 CATMOA식 입력 화면으로 개편하고, 화면을 다시
정리해 입력과 등록에 필요한 정보만 먼저 보이도록 했습니다. Microsoft 앱을 연상시키는
차분한 파란색을 공통 테마로 사용합니다.
사용자는 메시지를 복사·붙여넣거나 선택 텍스트를 가져오고, 문서·이미지를 작은 창에
드래그할 수 있습니다. Solar Pro 4가 일정과 할 일을 추출한 뒤, 기존 Outlook 내역과
겹치는 항목을 제외하고 카드에서 확인한 항목만 빠르게 등록합니다.

이번 UI 범위에서는 신규 등록 흐름에 집중했습니다. Solar Pro 4 분석은 앱의 FIFO
대기열에서 백그라운드로 실행하고, 분석이 끝난 결과만 검토창에 표시합니다. 검토창은
왼쪽 원문과 오른쪽 저장 요약을 나란히 보여주며, 기본 화면에는 등록에 필요한 정보만
남겼습니다.

- 쿨메신저 UDB 자동 탐색: 미포함
- 개인정보 탐지·마스킹: 미포함
- 기존 Outlook 내역 조회·수정·삭제 UI: 현재 범위에서 제외하고 후속 작업으로 보류

오른쪽 아래 상태 창은 `120×160`, 결과 확인창은 `760×620`으로 구성했습니다. 작은 창에서는
입력 상자와 실행 버튼을 모두 걷어내고 상태 한 줄만 남겼습니다. 입력은 전역 단축키·파일
드롭·`Ctrl+V`·`⋯` 메뉴로 들어오며, 창은 켜져 있는 동안 항상 위에 뜹니다. 앱을 멈추거나
끄는 길은 알림 영역 아이콘에 두었습니다. 결과 확인창은 그 작은 창을 가리지 않도록 화면
왼쪽 위에 자리를 잡습니다. 결과창은 왼쪽 원문을 수정 잠금으로 보여주고, 오른쪽 카드에는
제목·저장 대상·시점·조건·확인 사유만 먼저 표시합니다. 제목과 주요 날짜/시간은 카드에서
바로 편집하고, 상세 필드는 카드의 `상세`에서 엽니다.

## 2. 최종 파이프라인

```text
메시지 선택 / Ctrl+V / 텍스트 드래그 / 파일 드래그
        ↓
로컬 파일 텍스트 추출
  PDF · HWP · HWPX · DOCX · TXT · MD · CSV · TSV · 이미지 OCR
        ↓
FIFO 분석 대기열
        ↓
백그라운드 Solar Pro 4 구조화 JSON 분석
  API 키가 없거나 호출 실패 시 오프라인 규칙으로 전환
        ↓
SQLite에 분석 결과 pending 저장
        ↓
로컬 Outlook 미러로 중복·시간 겹침 확인
        ↓
왼쪽 원문 / 오른쪽 저장 요약 표시
  제목 · 시간 · 마감 · 조건 · 관련 시점 확인
        ↓
카드의 `등록`/`미등록`으로 한 건씩 처리
  분석이 끝난 다음 메시지는 같은 목록에 이어 붙음
        ↓
클래식 Outlook COM Calendar/Task 생성
        ↓
SQLite에 saved / failed 상태와 EntryID 기록
```

새 파일·텍스트는 현재 분석 또는 검토 결과를 덮어쓰지 않고 FIFO 대기열에 보관합니다.
분석은 검토창이 열려 있는 동안에도 백그라운드에서 계속 돌고, 끝난 결과는 기다리지 않고
검토창 목록에 `메시지 2`, `메시지 3`으로 이어 붙습니다. 아직 분석 중인 건수만 헤더의
`분석 중 n건`에 표시합니다. 남은 카드와 분석이 모두 없어지면 창이 닫힙니다.

## 3. 화면 사용법

### 3.1 설치와 실행

Windows와 클래식 Outlook이 설치되어 있어야 합니다.

```powershell
cd <project-folder>
python -m pip install -r requirements.txt
python app.py
```

Solar Pro 4를 사용하려면 프로젝트 루트 `.env` 또는 환경변수에 API 키를 넣습니다.

```text
UPSTAGE_API=up_...
UPSTAGE_MODEL=solar-pro4
```

API 키가 없으면 오프라인 규칙 분석으로 실행됩니다. 분석 중에도 다음 메시지를 바로
넣을 수 있습니다.

### 3.2 메시지 붙여넣기

1. Outlook 메일, 브라우저, 메신저 등에서 업무 메시지를 복사합니다.
2. 작은 창을 클릭해 포커스를 준 뒤 `Ctrl+V`를 누릅니다.
3. 곧바로 분석이 시작됩니다. 실행 버튼은 없습니다.

`⋯` 메뉴의 `붙여넣기 (Ctrl+V)`도 같은 동작입니다. tkinterdnd2가 설치된 환경에서는
다른 앱의 텍스트를 창으로 직접 드래그할 수도 있습니다.

### 3.3 선택 텍스트 바로 가져오기

1. 다른 프로그램에서 분석할 문장을 마우스로 선택합니다.
2. `Ctrl+Shift+X`를 누릅니다.
3. 선택 텍스트가 자동으로 앱에 들어가고 분석 창이 열립니다.

선택 텍스트 캡처가 어려운 프로그램에서는 먼저 `Ctrl+C`를 누른 뒤 앱 입력 영역에
붙여넣으면 됩니다.

### 3.4 파일 드래그앤드롭

1. 일정 내용이 들어 있는 파일을 준비합니다.
2. 파일을 작은 상태 창으로 드래그합니다. (창을 클릭하면 파일 선택 창이 열립니다)
3. 고양이가 내용을 읽은 뒤 분석 결과 창을 엽니다.

지원 파일:

- 문서: PDF, HWP, HWPX, DOCX
- 텍스트: TXT, Markdown, CSV, TSV, LOG
- 이미지: PNG, JPG, JPEG, BMP, GIF, WEBP, TIFF

파일 드롭이 동작하지 않는 환경에서는 `파일 선택` 버튼을 사용할 수 있습니다.
이미지 파일은 Python `pytesseract`뿐 아니라 Windows Tesseract 실행 파일과 한국어
언어팩도 설치되어 있어야 합니다. OCR 설정이 없으면 이미지 내용을 분석하지 않고
설치 안내를 표시합니다.

### 3.5 분석 결과 카드와 등록

분석 결과 창은 왼쪽에 수정 잠금 상태의 원문, 오른쪽에 저장할 요약을 보여줍니다.
제목·시점·조건·확인 사유만 먼저 보이며, 제목과 주요 날짜/시간은 카드에서 바로 편집할 수
있습니다. 상세 항목은 카드의 `상세`를 눌렀을 때 확인하고 편집할 수 있습니다.

- `담임 / 업무 / 참조` 분류
- 제목
- 일정 일시 또는 작업 마감일
- 장소, 알림, 반복 규칙
- Outlook에 들어갈 설명

카드마다 오른쪽에 `등록`과 `미등록` 버튼이 있습니다. 체크박스로 고른 뒤 화면 아래에서
한꺼번에 저장하던 흐름을 없애고, 한 건을 확인한 자리에서 바로 끝냅니다. 제목·주요
날짜·시간은 카드에서 바로 수정하고, 나머지 필드를 바꿀 때만 `상세`를 사용합니다.

- `등록`: 그 카드만 즉시 Outlook에 기록합니다. 편집한 값으로 다시 검사하므로,
  분석이 기본 선택을 해제한 조건부 항목도 사용자가 누르면 등록됩니다.
- `미등록`: 그 카드를 접어 둡니다. 되돌릴 수 있습니다.
- 아래 `모두 등록`: 분석이 안전하다고 본 항목만 한 번에 등록합니다. 조건부·확인 필요
  항목은 포함하지 않으므로 카드에서 직접 눌러야 합니다.

등록에 실패한 카드는 버튼이 다시 열려 수정 후 재시도할 수 있고, 클릭 시점에 중복으로
밝혀진 카드는 사유를 표시한 뒤 결정이 끝난 것으로 처리합니다.

### 행동 단위 추출 예시

다음 메시지는 날짜가 두 개지만 등록 업무는 하나입니다.

```text
토익 시험 감독관을 선착순으로 모집합니다.
일시: 2026. 8. 23 08:30
당일 오전 10시까지 접수 후 마감됩니다.
```

결과는 `토익 시험 감독관 신청` Todo 1건입니다. 모집은 조건부이므로 기본 선택을
해제하고, 시험일은 `event_context`, `당일 오전 10시`는 날짜 미확정 `due_time`으로
보존합니다. 시험 참석 Calendar나 `접수 마감 확인` Todo는 만들지 않습니다. 마감 날짜를
확인해 카드의 주요 마감 입력란에 바로 입력하면 해당 확인 사유가 해소됩니다.

## 4. 중복 및 시간 겹침 처리

카드를 표시하기 전과 등록 직전에 두 번 확인합니다. 두 검사 모두 Outlook을 직접 읽지
않고 로컬 미러(`outlook_mirror`)를 읽습니다. 카드마다 등록 버튼을 누르는 화면에서는
클릭할 때마다 폴더 전체를 COM으로 훑을 수 없기 때문입니다.

미러는 앱 시작 시 1회, 이후 5분마다 전용 COM 스레드가 전체를 다시 읽어 채웁니다.
이 앱이 만든 항목은 Outlook 기록 직후 미러에도 함께 씁니다(write-through). 따라서
연속으로 카드를 등록해도 직전에 만든 항목이 다음 검사에 곧바로 반영됩니다.
첫 동기화가 끝나기 전에는 예전처럼 Outlook을 직접 읽습니다.

그 대가로, 다른 프로그램이 만든 항목은 다음 동기화까지 미러에 없습니다. 최대 5분의
사각이 생기며, 이는 클릭 반응성과 맞바꾼 값입니다.

### Calendar

- 기존 Calendar 항목과 시간 구간이 겹치면 제외합니다.
- 종료 시간이 없는 일정은 60분 일정으로 간주합니다.
- 종일 일정은 하루 단위로 비교합니다.
- 같은 메시지에서 생성된 일정끼리 시간이 겹쳐도 뒤의 항목을 제외합니다.

### Todo

- 제목의 공백·구두점을 정리한 뒤 비교합니다.
- 제목이 같고 마감일이 같으면 제외합니다.
- 마감일이 없는 동일 제목 Todo도 중복으로 제외합니다.

제외된 카드는 `중복 제외` 사유를 표시합니다. 사용자가 제목이나 시간을 수정하면
제외 사유가 풀리고 다시 등록할 수 있습니다.

## 5. Outlook 등록 기준

| 분석 결과 | Outlook 대상 |
|---|---|
| 일시가 있는 업무 | Calendar |
| 마감일이 있는 업무 | 작업(Task) + 마감일, 그리고 마감일 당일 종일 표시 |
| 날짜가 없는 바로 할 일 | 작업(Task) |
| 참조 | Outlook에 등록하지 않음, SQLite에만 기록 |

Outlook 작업은 캘린더에 보이지 않으므로, 마감이 있는 작업은 `[마감] 제목` 종일
항목을 마감일에 하나 더 만듭니다. 범주 `마감`으로 표시하고 `한가함`으로 두며,
중복·시간 겹침 검사에서 제외합니다. 제외하지 않으면 마감일 하루가 통째로 막혀
그날의 실제 일정 등록이 모두 차단됩니다. 표시가 필요 없으면 카드 `상세`에서 끕니다.
표시 생성에 실패해도 이미 만들어진 작업은 되돌리지 않고 사유만 알립니다.

Outlook 등록은 `Outlook.Application` COM을 사용하는 로컬 자동화입니다.
Microsoft Graph나 메일 발송은 사용하지 않습니다. New Outlook만 설치된 환경에서는
동작하지 않을 수 있습니다.

## 6. 데이터 저장

DB 위치:

```text
data/work_items.sqlite3
```

주요 테이블:

- `inputs`: 사용자가 입력한 원문, 생성 시간, 기준일·시간대 등 `context_json`
- `work_items`: 분류 결과, 일정·마감 필드, `due_time`, 의미 정보 `semantic_json`,
  등록 상태, Outlook EntryID, 오류
- `outlook_mirror`: 현재 Outlook 프로필의 Calendar/작업 사본. 중복 검사가 읽는 표이며
  `origin`으로 동기화로 들어온 행과 이 앱이 쓴 행을 구분합니다
- `sync_state`: 마지막 미러 동기화 시각. 미러가 오래됐는지 판단하는 기준입니다

등록 상태는 다음과 같이 관리합니다.

```text
pending → saved
pending → failed
saved   → deleted   # 별도 관리 파이프라인에서 삭제된 경우
```

현재 요구사항에 따라 원문은 별도 PII 탐지·마스킹 없이 로컬 SQLite에 저장됩니다.
민감한 메시지를 다룰 때는 `.env`와 DB 파일을 외부에 공유하지 않아야 합니다.

## 7. 파일별 역할

| 파일 | 역할 |
|---|---|
| `app.py` | CATMOA식 메인 입력 창, 전역 단축키, 드롭 처리, FIFO 분석 대기열 |
| `file_input.py` | 드롭한 문서·이미지의 로컬 텍스트 추출 |
| `windows_drop.py` | Windows 네이티브 파일 드래그앤드롭 브리지 |
| `review.py` | 왼쪽 원문·오른쪽 저장 요약, 여러 메시지 누적, JSON 내보내기 |
| `cards.py` | 일정·할 일 편집 카드, 카드별 등록/미등록, 중복 제외 표시 |
| `service.py` | 의미 필드 검증, 기본 선택 정책, 중복 검사, SQLite 기록, Outlook 등록 조정 |
| `outlook_mirror.py` | Outlook 프로필의 로컬 사본, 백그라운드 동기화 스레드, write-through |
| `tray.py` | 알림 영역 아이콘. 창과 무관하게 일시중지·종료 |
| `icons.py` | `tray_icon.png` → `.ico` 변환, 창·작업 표시줄 아이콘 |
| `upstage_classifier.py` | Solar Pro 4 구조화 JSON 분류 |
| `outlook_adapter.py` | 클래식 Outlook Calendar/Task COM 연동 |
| `storage.py`, `models.py` | SQLite와 WorkItem 모델 |
| `selftest.py` | Outlook에 쓰지 않는 로컬·연결 검증 스크립트 |
| `test_review_flow.py` | Tk 위젯을 실제로 눌러 보는 검토창 회귀 검사 |

## 8. 검증 결과

다음 검증을 완료했습니다.

```text
python -m compileall -q .
→ 성공

python selftest.py --offline
→ 32/32 통과 (Outlook 미러 5건 포함)

python -m unittest test_review_flow
→ 8/8 통과 (Tk 위젯을 실제로 눌러 확인)

python -m unittest test_compact_extraction
→ 7/7 통과

python selftest.py
→ 30/30 통과 (Solar Pro 4 1건 포함, Outlook 기록 없음. 미러 추가 전 측정)

python -m pip check
→ No broken requirements found
```

추가로 다음을 확인했습니다.

- 메인 UI 초기화: 350×320
- 결과 확인창 초기화: 760×620
- 결과 확인창: 왼쪽 원문(수정 잠금) / 오른쪽 저장 요약 2열
- 결과 카드: 제목·주요 날짜/시간 직접 편집, `상세`로 나머지 필드 열기
- 작은 창: `350×160`, 항상 위, 상태 문구 5종(대기·처리 중·검토 중·검토 대기·일시중지) 확인
- 트레이: 아이콘 등록, `창 보이기`·`일시중지`/`재개`·`종료` 메뉴 동작, 종료 시 아이콘 제거
- 일시중지 중에는 단축키·드롭·붙여넣기 입력이 대기열에 들어가지 않음
- 검토창 위치: 작은 창이 있는 오른쪽 아래를 피해 왼쪽 위(24, 24)에 배치
- 마감일 표시: 작업과 함께 생성, EntryID 기록, 재등록 시 중복 생성 없음,
  그날의 다른 일정 등록을 막지 않음
- 카드 제목: 길면 줄을 늘려 끝까지 보이고, 짧으면 한 줄 유지. 붙여넣기처럼
  키 입력이 없는 편집도 저장값에 반영
- 결과 카드: `등록`은 그 카드 한 건만 서비스에 전달, `미등록`은 되돌리기 가능
- 한 메시지에 3건이 있을 때 카드마다 독립된 `등록`/`미등록` 동작 확인
- 등록 실패 카드는 버튼이 다시 열리고, 클릭 시점 중복 카드는 미결로 남지 않음
- 메시지 누적: 검토 중 도착한 두 번째 분석이 같은 창에 `메시지 2`로 이어 붙고,
  앞 메시지를 모두 처리하면 초점과 왼쪽 원문이 다음 메시지로 이동
- 창 종료 조건: 남은 카드와 분석이 모두 없을 때만 닫힘
- Outlook 미러: 실제 프로필 1회 동기화 13건 1.4초, 전용 COM 스레드에서 읽기 전용
- 미러 write-through: 등록 직후 같은 항목이 다음 중복 검사에서 걸림
- 미러 삭제 반영: Outlook에서 사라진 항목이 다음 동기화에서 미러에서도 제거
- 토익 모집 사례: 1개 후보, 조건부 선택 해제, 행사일·마감 시각 문맥 보존
- v2 의미 필드: 임시 SQLite 저장 후 재로딩 확인
- tkinterdnd2 텍스트·파일 드롭 등록 정상
- Windows 네이티브 파일 드롭 브리지 정상
- 파일 텍스트 로딩 정상
- 가상 Outlook 일정 겹침 및 Todo 중복 제외 정상
- 테스트 중 실제 Outlook 항목 생성·수정·삭제 없음

### 8.1 32건 Solar Pro 4 회귀 평가 (변경 전 기준선)

`test_set_30.xlsx`의 `테스트셋` 시트에서 실제 데이터 행 2~33을 사용했습니다.
본문은 G열, 발송일시는 F열에서 읽었고 Excel 내보내기 문자열의 `_x000D_`는
줄바꿈으로 복원했습니다. 각 요청에는 발송일시를 함께 전달하고 `solar-pro4`,
`reasoning_effort=none`으로 현재 구조화 프롬프트를 실행했습니다. 이 평가는
현재 프롬프트와 의미 필드의 회귀 기준선이며, 일반 메시지 전체의 일반화 성능으로
해석하지 않습니다.

실제 Outlook Calendar/Task 생성은 수행하지 않았습니다. API 추출 결과와 로컬
서비스 계약은 검증했지만, 외부 상태를 바꾸는 등록·수정·삭제 E2E는 별도 테스트
계정과 가짜 어댑터가 필요합니다.

#### 호출·응답 성능

- 최초 32회 시도: 29건 성공
- 최초 실패: 7번 JSON 파싱 오류, 9번·32번 시간 초과
- 실패 3건을 1회 재시도: 3/3건 성공
- 최종 응답 확보: 32/32건
- 성공 응답시간: 평균 23.38초, 중앙값 19.24초, 95백분위 50.41초, 최대 68.45초
- 20초 이내 응답: 16/32건, 30초 이내 응답: 23/32건

응답시간은 변경 전 코드에서 측정 중 60~90초로 임시 확장한 제한에서 측정했습니다.
당시 실제 제한은 20초였으므로 이 기준선에서는 응답의 절반가량이 오프라인 규칙으로
전환될 수 있었습니다. 앱의 FIFO 대기열은 응답 중에도 입력을 받을 수 있지만, 32건을
직렬 처리할 경우 평균 응답시간만 단순 적용해도 약 12분 30초가 걸립니다.

#### 의미 기준 판정

`통과`는 행동 수·대상·날짜 역할·등록 안전성이 모두 기준에 맞는 경우,
`부분`은 핵심 행동은 잡았으나 세부 보정이 필요한 경우, `개선 필요`는 잘못된
행동 생성·과다 분리·조건 누락·근거 없는 시각 추정으로 등록 오류 위험이 있는
경우입니다.

| Excel 행 | 실측 결과 | 판정 |
|---:|---|---|
| 2 | 학생 안내와 학생 신청을 2건으로 분리 | 개선 필요 |
| 3 | 완료된 송부 내용을 미완료 제출 업무로 생성 | 개선 필요 |
| 4 | 배부·상담 운영 2건. 기간 확인 사유 포함 | 부분 |
| 5 | 조건부 추천·회신 1건 | 통과 |
| 6 | 8/27 입력 1건. 체크리스트 누락 | 부분 |
| 7 | 중복 확인·회신 대신 출장 기안 제출 생성 | 개선 필요 |
| 8 | 참조·정보성 1건으로 등록 제외 | 통과 |
| 9 | 조건부 회신 1건과 관련 마감·행사 문맥. 마감 필드 변동 | 부분 |
| 10 | 가려진 시각을 14:00으로 추정한 Calendar | 개선 필요 |
| 11 | 충전·책상 배열 분리 결과가 반복마다 변동 | 부분 |
| 12 | 조건부 회신 1건, 마감 8/21 | 통과 |
| 13 | 문서 편철·회수 문서 처리 2건 | 통과 |
| 14 | 8/19 13:00 연수 1건 | 통과 |
| 15 | 신청 업무 1건과 체크리스트. 연도·시각 확인 필요 | 부분 |
| 16 | 연수 참석·감독 준비 2건. 권장 영상 분리 | 부분 |
| 17 | 2학년 안내 1건. 기본 마감일이 8/19로 설정 | 부분 |
| 18 | 8/25를 00:00 Calendar로 생성 | 개선 필요 |
| 19 | 반복 분리수거 지도 1건. 아침 시각 확인 필요 | 통과 |
| 20 | 배부 외 당일 재안내까지 별도 업무로 생성 | 개선 필요 |
| 21 | 항목 0건. 완료 안내 제외 | 통과 |
| 22 | 이수·시트 표시 1건과 체크리스트 | 통과 |
| 23 | 조건부 토익 신청 1건, 10:00 마감, 행사일 문맥 | 통과 |
| 24 | 참조·정보성 1건으로 등록 제외 | 통과 |
| 25 | 2·3·4교시 3건. 담당자·시각 확인 필요 | 부분 |
| 26 | 방학식 준비·개학일 재연결 2건과 체크리스트 | 통과 |
| 27 | 배부 1건. 가려진 4교시를 11:30으로 추정 | 부분 |
| 28 | 이수·실적 보완을 모두 필수 업무로 생성 | 개선 필요 |
| 29 | 제출 1건이지만 “있으면” 조건을 필수 처리 | 개선 필요 |
| 30 | 현황 조사 1건과 체크리스트. 화재 대응 업무는 제외 | 부분 |
| 31 | 학생 안내 1건. 설문 종료일 처리 보정 필요 | 부분 |
| 32 | 기대 2건보다 많은 3건 생성 | 개선 필요 |
| 33 | 항목 0건. 참조 안내 제외 | 통과 |

결과는 완전 통과 12건, 부분 통과 11건, 개선 필요 9건입니다. 특히 9·11·16·23번은
3회 반복했습니다. 토익 모집은 3회 모두 신청 Todo 1건을 유지했지만, 충전·책상
배열은 3회 중 2회만 2건 분리를 유지했고, 연수·감독은 권장 영상의 조건부 처리에
변동이 있었습니다.

### 8.2 피드백 반영 후 재평가

다음 변경을 적용한 뒤 같은 Excel 행 2~33을 발송일시와 함께 다시 Solar Pro 4에
전달했습니다.

- 요청 제한시간을 첫 시도 60초, 재시도 90초로 조정하고 최대 2회 시도
- `items`·항목 필수 필드·열거형·`all_day` 형식 검증
- 날짜만 있는 Calendar와 원문에 없는 수행 시각을 `start=null` 및 blocking 사유로 전환
- 모델이 `추정`, `불확실`, `MISSING_EXECUTION_TIME`, `relative_date` 등을 반환하면 등록 차단
- AI 실패 후 오프라인 규칙으로 전환된 후보는 기본 선택 해제
- Outlook 중복 조회에 실패하면 등록 자체를 보류

재평가 결과는 다음과 같습니다.

- 마지막 반복에서 31/32건 응답 확보, 1건은 60초·90초 재시도 후 시간 초과
- 성공 응답 평균 31.92초, 중앙값 27.62초, 95백분위 92.03초, 최대 97.53초
- 성공 응답 중 60초 이내 29/31건, 90초 이내 29/31건
- 원문에 없는 비종일 `00:00` Calendar 0건
- 불확실성으로 등록이 차단된 후보 9건

이 결과는 응답 안정성과 잘못된 시각의 자동 등록 위험이 줄었다는 뜻입니다. 항목을
너무 많이 나누거나, 안내·신청·조건부 행동의 의미를 잘못 구분하는 문제까지 해결했다는
뜻은 아닙니다. 이번 재평가에서는 변경 후 의미 기준 32건 전체를 다시 수동 채점하지
않았으므로, 한 번의 32/32 실행 결과와 변경 전의 12/11/9 수치는 새 정확도 점수로
사용하지 않습니다. API 시간 초과 시 실제 서비스는 오프라인 규칙 결과를 pending으로
남기고 기본 선택을 해제합니다.

## 9. 현재 제한사항과 다음 단계

- 기존 Outlook 조회·수정·삭제 UI는 현재 릴리즈에서 제외되어 후속 작업으로
  보류 상태입니다.
- 이미지 OCR은 Tesseract 실행 파일과 한국어 언어팩 설치가 필요합니다.
- Outlook 연동은 클래식 Outlook COM에 의존합니다.
- 32건 의미 회귀 기준에서는 자동 등록 품질을 최종 합격으로 볼 수 없습니다.
  날짜·교시·기간이 포함된 긴 공지의 과다 분리와 조건부 행동의 의미 변동은 남아
  있습니다. 시각이 원문에 없는 후보는 후처리로 차단하지만, 잘못된 행동 자체를
  모두 고치는 것은 아닙니다.
- 현재 API는 첫 요청 60초, 재시도 90초, 최대 2회로 동작합니다. 이전 버전의 최종
  성공 응답 최대는 97.53초였으며, 긴 대기열에서는 처리 시간이 길어질 수 있습니다.
- 중복·완료·참조 항목은 기본 카드 목록에서 접혀 있고 `참조/제외 n건 보기`에서
  사유를 확인할 수 있습니다. 제목·주요 날짜·시간은 카드에서 바로 고칠 수 있고, 원문 내용 자체가 바뀐 경우에만 다시 넣어 재분석합니다.
- 중복 검사는 로컬 미러를 읽으므로, 다른 프로그램이 Outlook에 만든 항목은 다음 동기화
  (최대 5분)까지 검사에 잡히지 않습니다. 카드마다 등록 버튼을 누르는 화면의 반응성과
  맞바꾼 값이며, 미러 동기화가 실패하면 작은 창에 그 사실을 표시합니다.
- 미러 동기화는 전체 재조회 방식입니다. 항목이 매우 많은 프로필에서는 5분마다의
  재조회 비용이 커질 수 있으며, `LastModificationTime` 기반 증분 동기화는 적용하지
  않았습니다.

현재 릴리즈의 종합 판정은 “신규 등록 UI·DB·대기열 구현 완료, 의미 추출은 검토형
베타”입니다. 무검토 자동 등록을 릴리즈하려면 위의 32건 개선 필요 사례를 먼저
해결하고 동일 fixture를 다시 평가해야 합니다.

## 10. 파일 정리 결과

현재 `app.py`에서 시작하는 실행 의존성 그래프에 포함되지 않는 파일은 프로젝트 루트에서
분리했습니다. 영구 삭제 대신 다음 보관 폴더로 이동하여 필요할 때 복구할 수 있도록 했습니다.

```text
<project-parent>/_archive/ms_automate_unused_20260907
```

스키마 마이그레이션 복구용 DB 사본은 다음에 보관했습니다.

```text
<project-parent>/_archive/ms_automate_db_backup_20260907/work_items.sqlite3
```

보관한 항목:

- `catmoa/` 참고 원본 프로젝트
- `command_view.py`, `outlook_command.py`, `outlook_view.py` 보류된 관리 UI/명령 모듈
- `solar_json.py` 이전 단독 CLI
- `cleanup_test_items.py` 정리용 일회성 스크립트
- `test_entries.json` 테스트 입력 fixture

`selftest.py`, 설정 파일, README/구현 리포트, SQLite DB는 현재 사용 중이거나 검증에 필요한
파일이므로 유지했습니다. Python이 실행 중 생성하는 `__pycache__/`는 소스 배포 대상이
아니며 `.gitignore`에 포함됩니다.

GitHub 게시 시 실제 메시지 원문이 들어 있는 `test_set_30.xlsx`, 사용자별 업무 정보가
들어 있는 `profile.json`, `.env`, 로컬 SQLite DB는 `.gitignore`로 제외했습니다.
배포본에는 `profile.example.json`만 포함합니다.

## 11. GitHub 게시 상태

비공개 저장소 [`dorororong/ms_automate`](https://github.com/dorororong/ms_automate)를
생성하고 `main` 브랜치를 push했습니다. CATMOA는 UI 참고 대상으로만 사용했으며,
원본 저장소에는 변경을 가하지 않았습니다.

```powershell
git remote -v
# origin  https://github.com/dorororong/ms_automate.git
git log -1 --oneline
# 8fe3d56 Implement CATMOA-style Outlook pipeline
```
